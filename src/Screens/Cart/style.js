import { StyleSheet } from 'react-native'

const productDetailsStyles =
  StyleSheet.create({
    qtyView: { flexDirection: "row", height: 25, marginTop: 5 }
  });

export default productDetailsStyles;
